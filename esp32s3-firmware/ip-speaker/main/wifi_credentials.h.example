#pragma once

// Copia este archivo a wifi_credentials.h (ya está en .gitignore, no se commitea)
// y rellena tus credenciales reales. wifi_credentials.h es temporal para el
// Hito 1 -- en el Hito 5 esto se sustituye por configuración vía NVS/portal AP.

#define WIFI_SSID "TU_SSID_AQUI"
#define WIFI_PASSWORD "TU_PASSWORD_AQUI"
